{
	"name": "langchain",
	// Same distro and Python major.minor version as used for running unit tests (we could also use the same exact image)
	"image": "mcr.microsoft.com/devcontainers/python:1-3.11-bullseye",
	// Same poetry version as used for running unit tests
	"features": {
		"ghcr.io/devcontainers-contrib/features/poetry:2": {
			"version": "1.4.2"
		}
	},
	// "forwardPorts": [],
	"postCreateCommand": "./.devcontainer/setup.sh"
	// "customizations": {},
	//"remoteUser": "root"
}