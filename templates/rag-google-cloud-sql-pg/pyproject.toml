[tool.poetry]
name = "rag-google-cloud-sql-pg"
version = "0.1.0"
description = "RAG using Cloud SQL for Postgres"
authors = ["Google LLC <googleapis-packages@google.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.9, ^3.11"
langchain = ">=0.2.5"
langchain-google-cloud-sql-pg = ">=0.10.0"
langchain-google-vertexai = ">=2.0.0"

[tool.poetry.group.dev.dependencies]
langchain-cli = ">=0.0.21"

[tool.langserve]
export_module = "rag_google_cloud_sql_pg"
export_attr = "chain"

[tool.templates-hub]
use-case = "rag"
author = "Google LLC"
integrations = ["PostgresVectorStore", "VertexAI", "VertexAIEmbeddings"]
tags = ["vectordbs"]

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
