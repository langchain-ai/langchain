I have traced this bug and verified the fix locally. 

**Approach:**
When `max_retries <= 0`, the current logic entirely omits the `retry_config` key from `client_kwargs`. The `openrouter` SDK treats omitted kwargs as `UNSET` and falls back to a 1-hour backoff strategy. 

To fix this, I will update `_build_client()` in `libs/partners/openrouter/langchain_openrouter/chat_models.py` to explicitly pass `retry_config=None` when `max_retries <= 0`, which actively overrides the SDK's fallback. I will also update the corresponding unit test (`test_max_retries_zero_disables_retries`) which currently asserts the broken omission behavior.

I already have the patch prepared and passing all 244 tests locally. Could a maintainer please assign this issue to me so the bot will allow the PR to be opened?
