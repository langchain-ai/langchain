"""Setup configuration for langchain-llamastack package."""

from setuptools import setup

# The setup.py file is now mainly for backwards compatibility
# Most configuration is now in pyproject.toml
setup()
