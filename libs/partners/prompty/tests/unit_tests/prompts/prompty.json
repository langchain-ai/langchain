{
  "default": {
    "type": "azure",
    "api_version": "2023-12-01-preview",
    "azure_endpoint": "${AZURE_OPENAI_ENDPOINT}",
    "azure_deployment": "${AZURE_OPENAI_DEPLOYMENT:gpt-35-turbo}",
    "api_key": "${AZURE_OPENAI_KEY}"
  }
}