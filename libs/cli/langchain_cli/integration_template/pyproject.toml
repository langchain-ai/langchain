[tool.poetry]
name = "__package_name__"
version = "0.0.1"
description = "An integration package connecting __integration_name__ and LangChain"
authors = []
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.8.1,<4.0"
langchain-core = ">=0.0.12"

[tool.poetry.group.dev.dependencies]
langchain-cli = ">=0.0.4"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
