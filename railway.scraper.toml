[build]
dockerfilePath = "Dockerfile.scraper"

[deploy]
startCommand = "python -m agents.scraper_agent.telegram_bot"
healthcheckPath = ""
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
numReplicas = 1
